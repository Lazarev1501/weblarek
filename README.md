# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Vite

Структура проекта:
- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:
- index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/main.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск
Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run dev
```

или

```
yarn
yarn dev
```
## Сборка

```
npm run build
```

или

```
yarn build
```
# Интернет-магазин «Web-Larёk»
«Web-Larёk» — это интернет-магазин с товарами для веб-разработчиков, где пользователи могут просматривать товары, добавлять их в корзину и оформлять заказы. Сайт предоставляет удобный интерфейс с модальными окнами для просмотра деталей товаров, управления корзиной и выбора способа оплаты, обеспечивая полный цикл покупки с отправкой заказов на сервер.

## Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

Model - слой данных, отвечает за хранение и изменение данных.  
View - слой представления, отвечает за отображение данных на странице.  
Presenter - презентер содержит основную логику приложения и  отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

### Базовый код

#### Класс Component
Является базовым классом для всех компонентов интерфейса.
Класс является дженериком и принимает в переменной `T` тип данных, которые могут быть переданы в метод `render` для отображения.

Конструктор:  
`constructor(container: HTMLElement)` - принимает ссылку на DOM элемент за отображение, которого он отвечает.

Поля класса:  
`container: HTMLElement` - поле для хранения корневого DOM элемента компонента.

Методы класса:  
`render(data?: Partial<T>): HTMLElement` - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от `Component` будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова `render` и записывать данные в необходимые DOM элементы.  
`setImage(element: HTMLImageElement, src: string, alt?: string): void` - утилитарный метод для модификации DOM-элементов `<img>`


#### Класс Api
Содержит в себе базовую логику отправки запросов.

Конструктор:  
`constructor(baseUrl: string, options: RequestInit = {})` - В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.

Поля класса:  
`baseUrl: string` - базовый адрес сервера  
`options: RequestInit` - объект с заголовками, которые будут использованы для запросов.

Методы:  
`get(uri: string): Promise<object>` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер  
`post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется `POST` запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.  
`handleResponse(response: Response): Promise<object>` - защищенный метод проверяющий ответ сервера на корректность и возвращающий объект с данными полученный от сервера или отклоненный промис, в случае некорректных данных.

#### Класс EventEmitter
Брокер событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

Конструктор класса не принимает параметров.

Поля класса:  
`_events: Map<string | RegExp, Set<Function>>)` -  хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

Методы класса:  
`on<T extends object>(event: EventName, callback: (data: T) => void): void` - подписка на событие, принимает название события и функцию обработчик.  
`emit<T extends object>(event: string, data?: T): void` - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.  
`trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.


##### Данные
В приложении используются две основные сущности: товар и покупатель.

# Интерфейс товара IProduct: описывает товар, который отображается в каталоге и может быть добавлен в корзину.

Поле        | Тип           | Описание                              

id          | string        | Уникальный идентификатор товара       
description | string        | Описание товара                       
image       | string        | Ссылка на изображение                 
title       | string        | Название товара                       
category    | string        | Категория товара                      
price       | number | null | Стоимость товара. Может отсутствовать 

# Интерфейс покупателя IBuyer: содержит данные покупателя, необходимые для оформления заказа.

Поле        | Тип           | Описание          

payment     | TPayment      | Способ оплаты     
email       | string        | Электронная почта 
phone       | string        | Номер телефона    
address     | string        | Адрес доставки    


###### Модели данных

# Класс CatalogModel
Класс отвечает за хранение и управление каталогом товаров.

Зона ответственности:
  хранение списка товаров;
  поиск товара по идентификатору;
  хранение выбранного товара для просмотра подробной информации.

Конструктор:
`constructor(products: IProduct[] = [])`        - В конструктор передается начальный список товаров.

Поля класса:
`protected products: IProduct[]`                - Массив всех товаров каталога.
`protected selectedProduct: IProduct | null`    - Товар, выбранный для детального просмотра.

Методы класса:
`setProducts(products: IProduct[]): void`       - Сохраняет список товаров.
`getProducts(): IProduct[]`                     - Возвращает массив товаров каталога.
`getProduct(id: string): IProduct | undefined`  - Возвращает товар по его идентификатору.
`setSelectedProduct(product: IProduct): void`   - Сохраняет выбранный товар.
`getSelectedProduct(): IProduct | null`         - Возвращает выбранный товар.


# Класс BasketModel
Класс отвечает за хранение товаров, выбранных покупателем.

Зона ответственности:
  хранение содержимого корзины;
  добавление и удаление товаров;
  подсчёт количества товаров;
  подсчёт общей стоимости заказа.

Конструктор:
`constructor(products: IProduct[] = [])`        - В конструктор передается начальное содержимое корзины.
	
Поля класса:
`protected products: IProduct[]`                - Массив товаров, находящихся в корзине.

Методы класса:
`getProducts(): IProduct[]`                     - Возвращает товары корзины.
`addProduct(product: IProduct): void`           - Добавляет товар в корзину.
`removeProduct(product: IProduct): void`        - Удаляет товар из корзины.
`clear(): void`                                 - Полностью очищает корзину.
`getTotalPrice(): number`                       - Возвращает суммарную стоимость товаров в корзине.
`getCount(): number`                            - Возвращает количество товаров в корзине.
`hasProduct(id: string): boolean`               - Проверяет наличие товара в корзине по идентификатору.


# Класс BuyerModel
Класс отвечает за хранение данных покупателя и их валидацию.

Зона ответственности:
  хранение контактных данных покупателя;
  хранение способа оплаты;
  изменение данных;
  очистка данных;
  проверка корректности заполнения формы заказа.

Конструктор:
`constructor(data?: Partial<IBuyer>)`           - В конструктор передаются начальные данные покупателя.

Поля класса:
`protected payment: TPayment | null;`           - Способ оплаты.
`protected address: string;`                    - Адрес доставки.
`protected email: string;`                      - Электронная почта покупателя.
`protected phone: string;`                      - Контактный телефон покупателя.

Методы класса:
`setData(data: Partial<IBuyer>): void`          - Обновляет переданные поля покупателя без изменения остальных значений.
`getData(): Partial<IBuyer>`                    - Возвращает все сохранённые данные покупателя.
`clear(): void`                                 - Очищает данные покупателя.
`validate(): Partial<Record<keyof IBuyer, string>>` - Проверяет корректность заполнения данных и возвращает объект ошибок.


###### Слой коммуникации

# Класс  WebLarekApi
Класс выполняет запрос на сервер с помощью метода get класса Api и получает с сервера объект с массивом товаров.

Зона ответственности:
  работа с конкретными эндпоинтами backend API (/product/, /order/)

Конструктор:
`constructor(private api: IApi)`                - В конструктор передается запрос

Методы класса:
`getProducts()`                                 - Получает список товаров с сервера.
`createOrder(order: IOrder)`                    - Создаёт заказ на сервере.



###### Model-View-Presenter (MVP)
Приложение разрабатывается с использованием архитектурного шаблона Model-View-Presenter (MVP). Данный подход разделяет приложение на три независимых компонента:
  Model (Модель) — отвечает за хранение данных, бизнес-логику и взаимодействие с внешними источниками данных.
  View (Представление) — отвечает за отображение пользовательского интерфейса и передачу действий пользователя презентеру.
  Presenter (Презентер) — выступает посредником между моделью и представлением: обрабатывает действия пользователя, получает данные из модели и подготавливает их для отображения.


# Интерфейс HeaderData
Интерфейс описывает данные, необходимые для отображения шапки сайта.

Поля интерфейса:
  counter: number — Количество товаров в корзине.

# Интерфейс GalleryData
Интерфейс описывает данные каталога товаров.

Поля интерфейса:
  catalog: HTMLElement[] — Массив DOM-элементов карточек товаров для отображения в каталоге.

# Интерфейс ICard
Интерфейс описывает данные товара, необходимые для отображения карточки.

Поля интерфейса:
  id: string — Уникальный идентификатор товара.
  title: string — Название товара.
  description: string — Описание товара.
  image: string — Ссылка на изображение товара.
  category: string — Категория товара.
  price: number | null — Стоимость товара.

# Интерфейс ICardActions
Интерфейс описывает действия, доступные для карточки товара.

Методы интерфейса:
`onClick(event: MouseEvent): void` — Обработчик нажатия на карточку или кнопку карточки.

# Интерфейс IBasketView
Интерфейс описывает данные корзины для отображения.

Поля интерфейса:
  items: HTMLElement[] — Список карточек товаров в корзине.
  total: number — Общая стоимость товаров.

# Интерфейс IModalData
Интерфейс описывает содержимое модального окна.

Поля интерфейса:
  content: HTMLElement — Компонент, отображаемый внутри модального окна.

# Интерфейс IFormState
Интерфейс описывает состояние формы.

Поля интерфейса:
  valid: boolean — Признак корректности заполнения формы.
  errors: string — Текст ошибок валидации.

# Интерфейс IOrderForm
Интерфейс описывает данные формы оформления заказа.

Поля интерфейса:
  payment: TPayment — Выбранный способ оплаты.
  address: string — Адрес доставки.

# Интерфейс IContactsForm
Интерфейс описывает данные формы контактной информации.

Поля интерфейса:
  email: string — Адрес электронной почты покупателя.
  phone: string — Контактный телефон покупателя.

# Интерфейс ISuccessData
Интерфейс описывает данные окна успешного оформления заказа.

Поля интерфейса:
  total: number — Сумма списанных синапсов.

# Интерфейс ISuccessActions
Интерфейс описывает действия окна успешного оформления заказа.

Методы интерфейса:
`onClick(): void` — Обработчик нажатия на кнопку закрытия сообщения.

# Интерфейс IEvents
Интерфейс брокера событий приложения.
Используется для взаимодействия между View, Presenter и Model.

Методы интерфейса:
`on(event: string, callback: Function): void` — Подписка на событие.
`off(event: string, callback: Function): void` — Отписка от события.
`emit(event: string, data?: unknown): void` — Генерация события.


## Класс Component
Класс является базовым для всех компонентов слоя представления и содержит общий функционал работы с DOM-элементами.

Зона ответственности:
  хранение корневого элемента компонента;
  обновление отображаемых данных;
  работа с изображениями;
  предоставление общего механизма отрисовки компонентов.

Конструктор:
`constructor(container: HTMLElement)` — В конструктор передаётся корневой DOM-элемент компонента.

Поля класса:
`protected container: HTMLElement` — Корневой элемент компонента.

Методы класса:
`render(data?: Partial<T>): HTMLElement` — Обновляет данные компонента и возвращает его корневой DOM-элемент.
`setImage(element: HTMLImageElement, src: string, alt?: string): void` — Устанавливает изображение и альтернативный текст для элемента.

## Класс Header
Класс отвечает за отображение шапки сайта и состояния корзины.

Зона ответственности:
  отображение кнопки корзины;
  отображение количества товаров в корзине;
  генерация события открытия корзины.

Конструктор:
`constructor(container: HTMLElement, events: IEvents)` — Принимает контейнер шапки и экземпляр брокера событий.

Поля класса:
`protected basketButton: HTMLButtonElement` — Кнопка открытия корзины.
`protected counterElement: HTMLElement` — Элемент отображения количества товаров в корзине.

Методы класса:
`set counter(value: number)` — Обновляет отображаемое количество товаров в корзине.

## Класс Gallery
Класс отвечает за отображение каталога товаров.

Зона ответственности:
  отображение списка карточек товаров;
  обновление содержимого каталога.

Конструктор:
`constructor(container: HTMLElement)` — Принимает контейнер каталога.

Поля класса:
`protected catalogElement: HTMLElement` — Контейнер для размещения карточек товаров.

Методы класса:
`set catalog(items: HTMLElement[])` — Обновляет список отображаемых карточек товаров.

## Класс Card
Базовый класс карточек товаров.

Зона ответственности:
  отображение информации о товаре;
  отображение изображения товара;
  отображение категории товара;
  отображение цены товара;
  хранение идентификатора товара.

Конструктор:
`constructor(container: HTMLElement, actions?: ICardActions)` — Принимает контейнер карточки и объект обработчиков действий.

Поля класса:
`protected titleElement: HTMLElement` — Название товара.
`protected priceElement: HTMLElement` — Цена товара.
`protected imageElement: HTMLImageElement` — Изображение товара.
`protected categoryElement: HTMLElement` — Категория товара.
`protected id: string` — Идентификатор товара.

Методы класса:
`set title(value: string)` — Устанавливает название товара.
`set price(value: number | null)` — Устанавливает цену товара.
`set image(value: string)` — Устанавливает изображение товара.
`set category(value: string)` — Устанавливает категорию товара.

## Класс CatalogCard
Класс отвечает за отображение карточки товара в каталоге.

Зона ответственности:
  отображение краткой информации о товаре;
  обработка выбора товара;
  генерация события открытия предпросмотра товара.

Конструктор:
`constructor(container: HTMLElement, actions?: ICardActions)` — Принимает шаблон карточки каталога.

## Класс PreviewCard
Класс отвечает за отображение подробной информации о товаре.

Зона ответственности:
  отображение полной информации о товаре;
  отображение описания товара;
  добавление товара в корзину;
  изменение состояния кнопки покупки.

Конструктор:
`constructor(container: HTMLElement, actions?: ICardActions)` — Принимает шаблон карточки предпросмотра.

Поля класса:
`protected descriptionElement: HTMLElement` — Описание товара.
`protected buttonElement: HTMLButtonElement` — Кнопка добавления в корзину.

Методы класса:
`set description(value: string)` — Устанавливает описание товара.
`set buttonText(value: string)` — Изменяет текст кнопки.
`set disabled(value: boolean)` — Управляет доступностью кнопки покупки.

## Класс BasketCard
Класс отвечает за отображение товара внутри корзины.

Зона ответственности:
  отображение товара в корзине;
  отображение порядкового номера товара;
  удаление товара из корзины.

Конструктор:
`constructor(container: HTMLElement, actions?: ICardActions)` — Принимает шаблон карточки корзины.

Поля класса:
`protected indexElement: HTMLElement` — Порядковый номер товара.
`protected deleteButton: HTMLButtonElement` — Кнопка удаления товара.

Методы класса:
`set index(value: number)` — Устанавливает номер товара в корзине.

## Класс Modal
Класс отвечает за работу модального окна.

Зона ответственности:
  открытие модального окна;
  закрытие модального окна;
  отображение содержимого модального окна;
  обработка клика по оверлею;
  обработка клика по кнопке закрытия.

Конструктор:
`constructor(container: HTMLElement, events: IEvents)` — Принимает контейнер модального окна и брокер событий.

Поля класса:
`protected closeButton: HTMLButtonElement` — Кнопка закрытия окна.
`protected contentElement: HTMLElement` — Контейнер содержимого окна.

Методы класса:
`open(): void` — Открывает модальное окно.
`close(): void` — Закрывает модальное окно.
`set content(value: HTMLElement)` — Устанавливает содержимое модального окна.
`render(data: { content: HTMLElement }): HTMLElement` — Отображает переданное содержимое в модальном окне.

## Класс Basket
Класс отвечает за отображение корзины пользователя.

Зона ответственности:
  отображение списка товаров в корзине;
  отображение общей стоимости заказа;
  отображение кнопки оформления заказа.

Конструктор:
`constructor(container: HTMLElement, events: IEvents)` — Принимает шаблон корзины и брокер событий.

Поля класса:
`protected listElement: HTMLElement` — Контейнер списка товаров.
`protected priceElement: HTMLElement` — Элемент отображения общей стоимости.
`protected orderButton: HTMLButtonElement` — Кнопка оформления заказа.

Методы класса:
`set items(items: HTMLElement[])` — Обновляет список товаров в корзине.
`set total(value: number)` — Обновляет итоговую стоимость заказа.

## Класс Form
Базовый класс для всех форм приложения.

Зона ответственности:
  обработка ввода данных пользователем;
  отображение ошибок валидации;
  управление доступностью кнопки отправки;
  генерация событий изменения данных формы.

Конструктор:
`constructor(container: HTMLFormElement, events: IEvents)` — Принимает форму и брокер событий.

Поля класса:
`protected submitButton: HTMLButtonElement` — Кнопка отправки формы.
`protected errorsElement: HTMLElement` — Блок отображения ошибок.

Методы класса:
`set valid(value: boolean)` — Управляет доступностью кнопки отправки.
`set errors(value: string)` — Отображает сообщения об ошибках.
`render(state: Partial<T>): HTMLElement` — Обновляет состояние формы.

## Класс OrderForm
Класс отвечает за отображение формы оформления заказа.

Зона ответственности:
  выбор способа оплаты;
  ввод адреса доставки;
  отображение ошибок заполнения формы.

Конструктор:
`constructor(container: HTMLFormElement, events: IEvents)` — Принимает форму заказа и брокер событий.

Поля класса:
`protected addressInput: HTMLInputElement` — Поле ввода адреса.
`protected paymentButtons: HTMLButtonElement[]` — Кнопки выбора способа оплаты.

Методы класса:
`set address(value: string)` — Устанавливает адрес доставки.
`set payment(value: TPayment)` — Устанавливает выбранный способ оплаты.

## Класс ContactsForm
Класс отвечает за отображение формы контактных данных покупателя.

Зона ответственности:
  ввод адреса электронной почты;
  ввод номера телефона;
  отображение ошибок заполнения формы.

Конструктор:
`constructor(container: HTMLFormElement, events: IEvents)` — Принимает форму контактов и брокер событий.

Поля класса:
`protected emailInput: HTMLInputElement` — Поле ввода электронной почты.
`protected phoneInput: HTMLInputElement` — Поле ввода номера телефона.

Методы класса:
`set email(value: string)` — Устанавливает адрес электронной почты.
`set phone(value: string)` — Устанавливает номер телефона.

## Класс Success
Класс отвечает за отображение сообщения об успешном оформлении заказа.

Зона ответственности:
  отображение информации об успешном заказе;
  отображение суммы списанных синапсов;
  обработка закрытия сообщения.

Конструктор:
`constructor(container: HTMLElement, actions: ISuccessActions)` — Принимает шаблон окна успешного оформления заказа.

Поля класса:
`protected descriptionElement: HTMLElement` — Элемент отображения суммы заказа.
`protected closeButton: HTMLButtonElement` — Кнопка закрытия сообщения.

Методы класса:
`set total(value: number)` — Устанавливает сумму списанных синапсов.
`render(data: { total: number }): HTMLElement` — Отображает информацию о завершённом заказе.


##### События приложения

# Событие basket:open
Генерируется компонентом Header при нажатии пользователем на кнопку корзины.
Источник: Header
Данные: не передаются.
Назначение: открыть модальное окно корзины.

# Событие card:select
Генерируется компонентом CatalogCard при выборе товара в каталоге.
Источник: CatalogCard
Данные:
{
    id: string
}
Назначение: открыть окно подробного просмотра товара.

# Событие basket:add
Генерируется компонентом PreviewCard при нажатии кнопки «В корзину».
Источник: PreviewCard
Данные:
{
    id: string
}
Назначение: добавить товар в корзину.

# Событие basket:remove
Генерируется компонентом BasketCard при нажатии кнопки удаления товара.
Источник: BasketCard
Данные:
{
    id: string
}
Назначение: удалить товар из корзины.

# Событие order:start
Генерируется компонентом Basket при нажатии кнопки «Оформить».
Источник: Basket
Данные: не передаются.
Назначение: открыть форму оформления заказа.

# Событие payment:change
Генерируется компонентом OrderForm при выборе способа оплаты.
Источник: OrderForm
Данные:
{
    payment: TPayment
}
Назначение: обновить данные заказа.

# Событие address:change
Генерируется компонентом OrderForm при изменении адреса доставки.
Источник: OrderForm
Данные:
{
    address: string
}
Назначение: сохранить адрес доставки в модели данных.

# Событие order:submit
Генерируется компонентом OrderForm после отправки формы заказа.
Источник: OrderForm
Данные:
{
    payment: TPayment;
    address: string;
}
Назначение: перейти к заполнению контактных данных.

# Событие email:change
Генерируется компонентом ContactsForm при изменении поля электронной почты.
Источник: ContactsForm
Данные:
{
    email: string
}
Назначение: обновить данные покупателя.

# Событие phone:change
Генерируется компонентом ContactsForm при изменении номера телефона.
Источник: ContactsForm
Данные:
{
    phone: string
}
Назначение: обновить данные покупателя.

# Событие contacts:submit
Генерируется компонентом ContactsForm после отправки формы контактных данных.
Источник: ContactsForm
Данные:
{
    email: string;
    phone: string;
}
Назначение: инициировать оформление заказа.

# Событие modal:open
Генерируется компонентом Modal при открытии модального окна.
Источник: Modal
Данные: не передаются.
Назначение: уведомить приложение об открытии модального окна.

# Событие modal:close
Генерируется компонентом Modal при закрытии модального окна.
Источник: Modal
Данные: не передаются.
Назначение: уведомить приложение о закрытии модального окна.

# Событие success:close
Генерируется компонентом Success при нажатии кнопки «За новыми покупками!».
Источник: Success
Данные: не передаются.
Назначение: закрыть окно успешного оформления заказа и вернуть пользователя к каталогу.